"use client";
import { useState, FC, ReactNode } from 'react';
import Head from 'next/head';
import { motion, AnimatePresence } from 'framer-motion';
import { FaStethoscope, FaHeartbeat, FaNotesMedical, FaUserMd, FaWhatsapp, FaLinkedin, FaChevronDown } from 'react-icons/fa';
import { MdLocationOn, MdEmail, MdPhone, MdAccessTime } from 'react-icons/md';

// --- Tipos (Opcional, mas boa prática com TypeScript) ---
interface Specialty {
  icon: ReactNode;
  title: string;
  description: string;
}

interface Testimonial {
  quote: string;
  name: string;
  info: string;
}

interface FAQItem {
  question: string;
  answer: string;
}

// --- Dados de Exemplo (Facilita a manutenção) ---
const specialtiesData: Specialty[] = [
  {
    icon: <FaStethoscope className="w-12 h-12 text-teal-600" />,
    title: 'Consultas Clínicas',
    description: 'Avaliação completa da sua saúde, com foco em diagnóstico preciso e um plano de tratamento individualizado.'
  },
  {
    icon: <FaHeartbeat className="w-12 h-12 text-teal-600" />,
    title: 'Check-up e Prevenção',
    description: 'Realização de exames de rotina e check-ups completos para a prevenção de doenças e promoção da saúde.'
  },
  {
    icon: <FaNotesMedical className="w-12 h-12 text-teal-600" />,
    title: 'Acompanhamento de Crônicos',
    description: 'Manejo e acompanhamento contínuo de condições crônicas como hipertensão, diabetes e outras.'
  },
];

const testimonialsData: Testimonial[] = [
  {
    quote: 'Um profissional exemplar! O Dr(a). me ouviu com atenção e explicou tudo sobre meu tratamento. Sinto-me muito mais seguro e bem cuidado.',
    name: 'Roberto Guimarães',
    info: 'Paciente desde 2022'
  },
  {
    quote: 'Atendimento humano e de altíssima qualidade. A consulta foi pontual e o consultório é impecável. Recomendo a todos.',
    name: 'Lúcia Andrade',
    info: 'Paciente'
  },
  {
    quote: 'Finalmente encontrei um médico que se importa de verdade. Graças ao acompanhamento, minha saúde melhorou significativamente.',
    name: 'Fernando Costa',
    info: 'Paciente de acompanhamento crônico'
  }
];

const faqData: FAQItem[] = [
  {
    question: 'Você aceita convênios médicos?',
    answer: 'Sim, atendo uma ampla variedade de convênios. Para confirmar se o seu plano é aceito, por favor, entre em contato com minha secretária pelo WhatsApp ou telefone.'
  },
  {
    question: 'Como é a primeira consulta?',
    answer: 'Na primeira consulta, realizamos uma anamnese detalhada para entender seu histórico de saúde, queixas atuais e estilo de vida. Com base nisso, podemos solicitar exames e traçar um plano de cuidado inicial.'
  },
  {
    question: 'Preciso de um encaminhamento para agendar?',
    answer: 'Não é necessário um encaminhamento para agendar sua primeira consulta. Você pode entrar em contato diretamente conosco para marcar seu horário.'
  },
  {
    question: 'Quais são os horários de atendimento?',
    answer: 'O consultório funciona de segunda a sexta, das 8h às 18h. Para agendamentos, recomendamos entrar em contato durante este período para uma resposta mais rápida.'
  }
];

// --- Componentes da Página ---

const Header: FC = () => {
  return (
    <header className="bg-white/95 backdrop-blur-sm shadow-sm sticky top-0 z-50">
      <div className="container mx-auto px-6 py-4 flex justify-between items-center">
        <div className="text-xl font-bold text-gray-800">
          <FaUserMd className="inline-block mr-2 text-teal-700" />
          Dr(a). Seu Nome | Especialidade
        </div>
        <nav className="hidden md:flex space-x-8">
          <a href="#especialidades" className="text-gray-600 hover:text-teal-700 transition-colors">Especialidades</a>
          <a href="#sobre" className="text-gray-600 hover:text-teal-700 transition-colors">Sobre Mim</a>
          <a href="#contato" className="text-gray-600 hover:text-teal-700 transition-colors">Contato</a>
        </nav>
        <a 
          href="https://wa.me/5500000000000" // Coloque seu número de WhatsApp aqui
          target="_blank"
          rel="noopener noreferrer"
          className="bg-teal-600 hover:bg-teal-700 text-white font-bold py-2 px-4 rounded-full transition-transform transform hover:scale-105"
        >
          Agendar Consulta
        </a>
      </div>
    </header>
  );
};

const Hero: FC = () => {
  return (
    <section className="bg-slate-50">
      <div className="container mx-auto px-6 py-20 grid md:grid-cols-2 gap-12 items-center">
        <motion.div
          initial={{ opacity: 0, x: -50 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.8 }}
        >
          <h1 className="text-4xl md:text-5xl font-extrabold text-gray-800 leading-tight">
            Cuidando da sua saúde com dedicação e expertise.
          </h1>
          <p className="mt-4 text-lg text-gray-600">
            Atendimento médico humanizado e focado no seu bem-estar integral. Sua saúde é a nossa prioridade.
          </p>
          <motion.a 
            href="#contato"
            className="mt-8 inline-block bg-teal-600 hover:bg-teal-700 text-white font-bold py-3 px-8 rounded-full text-lg transition-transform transform hover:scale-105 shadow-lg"
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
          >
            Conheça o Consultório
          </motion.a>
        </motion.div>
        <motion.div
          initial={{ opacity: 0, scale: 0.8 }}
          animate={{ opacity: 1, scale: 1 }}
          transition={{ duration: 0.8, delay: 0.2 }}
          className="hidden md:flex justify-center"
        >
          <img src="/medico-hero.jpg" alt="Médico(a) em ambiente de consultório" className="rounded-lg shadow-2xl w-full max-w-md object-cover" />
        </motion.div>
      </div>
    </section>
  );
};

const Section: FC<{ id: string; title: string; subtitle: string; children: ReactNode; bgClassName?: string }> = ({ id, title, subtitle, children, bgClassName = 'bg-white' }) => (
    <div className={bgClassName}>
        <motion.section
            id={id}
            className="container mx-auto px-6 py-20"
            initial={{ opacity: 0, y: 50 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, amount: 0.3 }}
            transition={{ duration: 0.7 }}
        >
            <h2 className="text-3xl font-bold text-center text-gray-800">{title}</h2>
            <p className="text-center text-gray-600 mt-2 mb-12 max-w-2xl mx-auto">{subtitle}</p>
            {children}
        </motion.section>
    </div>
);

const Specialties: FC = () => {
  return (
    <Section
      id="especialidades"
      title="Especialidades e Tratamentos"
      subtitle="Oferecendo diagnósticos precisos e tratamentos eficazes, com base na ciência e no cuidado humano."
    >
      <div className="grid md:grid-cols-3 gap-8">
        {specialtiesData.map((specialty, index) => (
          <motion.div 
            key={index}
            className="bg-slate-50 p-8 rounded-lg text-center hover:shadow-xl hover:-translate-y-2 transition-all duration-300 border-t-4 border-teal-500"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, amount: 0.5 }}
            transition={{ duration: 0.5, delay: index * 0.1 }}
          >
            <div className="flex justify-center mb-4">{specialty.icon}</div>
            <h3 className="text-xl font-bold text-gray-800 mb-2">{specialty.title}</h3>
            <p className="text-gray-600">{specialty.description}</p>
          </motion.div>
        ))}
      </div>
    </Section>
  );
};

const About: FC = () => {
  return (
    <Section id="sobre" title="Sobre o Dr(a). Seu Nome" subtitle="Conheça minha trajetória e filosofia de trabalho." bgClassName="bg-slate-50">
       <div className="container mx-auto px-6 grid md:grid-cols-2 gap-16 items-center">
        <motion.div
            initial={{ opacity: 0, scale: 0.9 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true }}
            transition={{ duration: 0.7 }}
            className="flex justify-center"
        >
            <img src="/medico-perfil.jpg" alt="Foto profissional do Dr(a). Seu Nome" className="rounded-full shadow-xl w-80 h-80 object-cover" />
        </motion.div>
        <motion.div
            initial={{ opacity: 0, x: 50 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.7, delay: 0.2 }}
        >
            <h3 className="text-2xl font-bold text-teal-800 mb-4">Uma vida dedicada à medicina</h3>
            <p className="text-gray-600 mb-4">
                Formado(a) pela [Nome da Universidade], com residência em [Sua Especialidade] no [Nome do Hospital], minha paixão é aplicar o conhecimento científico para melhorar a vida dos meus pacientes. Acredito em uma medicina baseada em evidências, mas sem nunca esquecer da empatia e do diálogo.
            </p>
            <p className="text-gray-600">
                Meu objetivo é criar uma relação de confiança, onde você se sinta à vontade para expor suas dúvidas e participar ativamente do seu tratamento.
            </p>
            <p className="font-bold text-gray-700 mt-4">CRM/UF 123456</p>
        </motion.div>
       </div>
    </Section>
  );
};

const Testimonials: FC = () => {
  return (
    <Section id="depoimentos" title="O que meus pacientes dizem" subtitle="A confiança e o bem-estar de quem eu atendo são minha maior satisfação.">
        <div className="grid lg:grid-cols-3 gap-8">
            {testimonialsData.map((testimonial, index) => (
                <motion.div 
                    key={index}
                    className="bg-white p-6 rounded-lg shadow-lg border-l-4 border-teal-500"
                    initial={{ opacity: 0, y: 50 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    viewport={{ once: true, amount: 0.5 }}
                    transition={{ duration: 0.5, delay: index * 0.1 }}
                >
                    <p className="text-gray-600 italic">{testimonial.quote}</p>
                    <div className="mt-4 text-right">
                        <p className="font-bold text-gray-800">{testimonial.name}</p>
                        <p className="text-sm text-teal-700">{testimonial.info}</p>
                    </div>
                </motion.div>
            ))}
        </div>
    </Section>
  );
};

const FAQ: FC = () => {
  const [openIndex, setOpenIndex] = useState<number | null>(null);

  return (
    <Section id="faq" title="Dúvidas Comuns" subtitle="Respostas para as perguntas mais frequentes sobre minhas consultas." bgClassName="bg-slate-50">
        <div className="max-w-3xl mx-auto">
            {faqData.map((faq, index) => (
                <div key={index} className="border-b border-gray-200 py-4">
                    <button
                        onClick={() => setOpenIndex(openIndex === index ? null : index)}
                        className="w-full flex justify-between items-center text-left text-lg font-semibold text-gray-800"
                    >
                        <span>{faq.question}</span>
                        <motion.div animate={{ rotate: openIndex === index ? 180 : 0 }} transition={{ duration: 0.3 }}>
                            <FaChevronDown className="text-teal-600" />
                        </motion.div>
                    </button>
                    <AnimatePresence>
                        {openIndex === index && (
                            <motion.div
                                initial={{ opacity: 0, height: 0, marginTop: 0 }}
                                animate={{ opacity: 1, height: 'auto', marginTop: '16px' }}
                                exit={{ opacity: 0, height: 0, marginTop: 0 }}
                                transition={{ duration: 0.3, ease: "easeInOut" }}
                            >
                                <p className="text-gray-600">{faq.answer}</p>
                            </motion.div>
                        )}
                    </AnimatePresence>
                </div>
            ))}
        </div>
    </Section>
  );
}

const Contact: FC = () => {
    return (
        <Section id="contato" title="Agende Sua Consulta" subtitle="Estou à disposição para cuidar da sua saúde. Entre em contato para marcar seu horário.">
            <div className="grid md:grid-cols-2 gap-12 items-start max-w-5xl mx-auto">
                <motion.div 
                    className="bg-white p-8 rounded-lg shadow-xl"
                    initial={{ opacity: 0, x: -50 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    viewport={{ once: true }}
                    transition={{ duration: 0.7 }}
                >
                    <h3 className="text-2xl font-bold text-teal-800 mb-6">Informações do Consultório</h3>
                    <div className="space-y-4 text-gray-700">
                        <p className="flex items-start"><MdLocationOn className="w-6 h-6 mr-3 text-teal-600 flex-shrink-0 mt-1"/> Av. da Saúde, 123, Sala 101 <br/> Bairro Clínico, Sua Cidade - UF</p>
                        <p className="flex items-center"><MdPhone className="w-6 h-6 mr-3 text-teal-600"/> (99) 3232-4545</p>
                        <p className="flex items-center"><MdEmail className="w-6 h-6 mr-3 text-teal-600"/> contato@drs_eunome.com.br</p>
                        <p className="flex items-center"><MdAccessTime className="w-6 h-6 mr-3 text-teal-600"/> Seg - Sex: 08:00 - 18:00</p>
                    </div>
                     <a 
                        href="https://wa.me/5500000000000" // Coloque seu número de WhatsApp aqui
                        target="_blank"
                        rel="noopener noreferrer"
                        className="mt-8 w-full text-center inline-block bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-8 rounded-lg text-lg transition-transform transform hover:scale-105 shadow-lg"
                    >
                       <FaWhatsapp className="inline-block mr-2 mb-1" /> Agendar pelo WhatsApp
                    </a>
                </motion.div>
                <motion.div 
                    className="rounded-lg shadow-xl overflow-hidden h-full"
                    initial={{ opacity: 0, scale: 0.9 }}
                    whileInView={{ opacity: 1, scale: 1 }}
                    viewport={{ once: true }}
                    transition={{ duration: 0.7 }}
                >
                     <img src="/mapa-consultorio.png" alt="Localização do consultório no mapa" className="w-full h-full object-cover min-h-[400px]" />
                </motion.div>
            </div>
        </Section>
    )
}

const Footer: FC = () => {
    return (
        <footer className="bg-gray-800 text-gray-300">
            <div className="container mx-auto px-6 py-6 text-center md:flex justify-between items-center">
                <p className="text-sm">
                    &copy; {new Date().getFullYear()} Dr(a). Seu Nome | CRM/UF 123456 <br/>
                    <span className="text-xs text-gray-400">As informações contidas neste website têm caráter informativo e educacional.</span>
                </p>
                <div className="flex justify-center space-x-4 mt-4 md:mt-0">
                    <a href="#" aria-label="LinkedIn" className="hover:text-white transition-colors"><FaLinkedin size={22} /></a>
                    <a href="https://wa.me/5500000000000" target="_blank" rel="noopener noreferrer" aria-label="WhatsApp" className="hover:text-white transition-colors"><FaWhatsapp size={22} /></a>
                </div>
            </div>
        </footer>
    );
};

// --- Página Principal ---
const MedicoLandingPage: FC = () => {
  return (
    <>
      <Head>
        <title>Dr(a). Seu Nome - Especialidade | Cuidado e Saúde</title>
        <meta name="description" content="Consultório médico do(a) Dr(a). Seu Nome, especialista em [Sua Especialidade]. Agende sua consulta e cuide da sua saúde com um atendimento dedicado e humanizado." />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <div className="bg-white font-sans antialiased">
        <Header />
        <main>
          <Hero />
          <Specialties />
          <About />
          <Testimonials />
          <FAQ />
          <Contact />
        </main>
        <Footer />
      </div>
    </>
  );
};

export default MedicoLandingPage;