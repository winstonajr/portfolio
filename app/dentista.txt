"use client"
import { useState, FC, ReactNode } from 'react';
import Head from 'next/head';
import { motion, AnimatePresence } from 'framer-motion';
import { FaTooth, FaStethoscope, FaUserMd, FaWhatsapp, FaInstagram, FaFacebook, FaChevronDown } from 'react-icons/fa';
import { MdLocationOn, MdEmail, MdPhone } from 'react-icons/md';

// --- Tipos (Opcional, mas boa prática com TypeScript) ---
interface Service {
  icon: ReactNode;
  title: string;
  description: string;
}

interface Testimonial {
  quote: string;
  name: string;
  role: string;
}

interface FAQItem {
  question: string;
  answer: string;
}

// --- Dados de Exemplo (Facilita a manutenção) ---
const servicesData: Service[] = [
  {
    icon: <FaTooth className="w-12 h-12 text-cyan-500" />,
    title: 'Clareamento Dental',
    description: 'Recupere o branco natural dos seus dentes com nossas técnicas avançadas e seguras de clareamento.'
  },
  {
    icon: <FaUserMd className="w-12 h-12 text-cyan-500" />,
    title: 'Implantes Dentários',
    description: 'Soluções permanentes e estéticas para a substituição de dentes perdidos, restaurando seu sorriso e função mastigatória.'
  },
  {
    icon: <FaStethoscope className="w-12 h-12 text-cyan-500" />,
    title: 'Check-up Preventivo',
    description: 'A prevenção é o melhor tratamento. Agende seu check-up regular para manter sua saúde bucal em dia.'
  },
];

const testimonialsData: Testimonial[] = [
  {
    quote: 'Atendimento incrível e profissionais super qualificados! Meu sorriso nunca esteve tão bonito. Recomendo de olhos fechados!',
    name: 'Maria Silva',
    role: 'Paciente Satisfeita'
  },
  {
    quote: 'Tinha muito medo de dentista, mas a equipe me deixou super à vontade. O tratamento foi indolor e o resultado, perfeito.',
    name: 'João Pereira',
    role: 'Paciente Satisfeito'
  },
  {
    quote: 'A clínica é moderna, limpa e o atendimento é pontual. Todos os procedimentos foram explicados com clareza. Excelente!',
    name: 'Ana Costa',
    role: 'Paciente Satisfeita'
  }
];

const faqData: FAQItem[] = [
  {
    question: 'Vocês aceitam planos de saúde?',
    answer: 'Sim, aceitamos uma vasta gama de planos de saúde. Por favor, entre em contato com nossa recepção para verificar a cobertura do seu plano específico e obter mais detalhes.'
  },
  {
    question: 'Qual é o horário de funcionamento?',
    answer: 'Nossa clínica funciona de Segunda a Sexta, das 8h às 18h, e aos Sábados, das 9h às 13h. Estamos fechados aos domingos e feriados.'
  },
  {
    question: 'O procedimento de clareamento dental é seguro?',
    answer: 'Totalmente seguro. Utilizamos produtos aprovados pela ANVISA e técnicas modernas que são realizadas sob a supervisão de nossos dentistas qualificados, garantindo a segurança e a eficácia do tratamento.'
  },
  {
    question: 'Como posso agendar uma consulta?',
    answer: 'Você pode agendar sua consulta de forma rápida e fácil através do nosso botão de WhatsApp nesta página, ou ligando para o nosso número de telefone. Nossa equipe de recepção terá o prazer em ajudá-lo a encontrar o melhor horário.'
  }
];


// --- Componentes da Página ---

const Header: FC = () => {
  return (
    <header className="bg-white/80 backdrop-blur-md shadow-sm sticky top-0 z-50">
      <div className="container mx-auto px-6 py-4 flex justify-between items-center">
        <div className="text-2xl font-bold text-gray-800">
          Dr(a). Seu Nome
        </div>
        <nav className="hidden md:flex space-x-8">
          <a href="#servicos" className="text-gray-600 hover:text-cyan-600 transition-colors">Serviços</a>
          <a href="#sobre" className="text-gray-600 hover:text-cyan-600 transition-colors">Sobre</a>
          <a href="#contato" className="text-gray-600 hover:text-cyan-600 transition-colors">Contato</a>
        </nav>
        <a 
          href="https://wa.me/5500000000000" // Coloque seu número de WhatsApp aqui
          target="_blank"
          rel="noopener noreferrer"
          className="bg-cyan-500 hover:bg-cyan-600 text-white font-bold py-2 px-4 rounded-full transition-transform transform hover:scale-105"
        >
          Agendar Consulta
        </a>
      </div>
    </header>
  );
};

const Hero: FC = () => {
  return (
    <section className="bg-gray-50">
      <div className="container mx-auto px-6 py-20 grid md:grid-cols-2 gap-12 items-center">
        <motion.div
          initial={{ opacity: 0, x: -50 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.8 }}
        >
          <h1 className="text-4xl md:text-5xl font-extrabold text-gray-800 leading-tight">
            O sorriso perfeito que você sempre sonhou, agora ao seu alcance.
          </h1>
          <p className="mt-4 text-lg text-gray-600">
            Tecnologia de ponta e atendimento humanizado para cuidar da sua saúde bucal com excelência.
          </p>
          <motion.a 
            href="https://wa.me/5500000000000" // Coloque seu número de WhatsApp aqui
            target="_blank"
            rel="noopener noreferrer"
            className="mt-8 inline-block bg-cyan-500 hover:bg-cyan-600 text-white font-bold py-3 px-8 rounded-full text-lg transition-transform transform hover:scale-105 shadow-lg"
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
          >
            Fale Conosco no WhatsApp
          </motion.a>
        </motion.div>
        <motion.div
          initial={{ opacity: 0, scale: 0.8 }}
          animate={{ opacity: 1, scale: 1 }}
          transition={{ duration: 0.8, delay: 0.2 }}
          className="hidden md:block"
        >
          {/* Use uma imagem real aqui */}
          <img src="/hero-image.jpg" alt="Mulher sorrindo" className="rounded-lg shadow-2xl" />
        </motion.div>
      </div>
    </section>
  );
};

const Section: FC<{ id: string; title: string; subtitle: string; children: ReactNode; className?: string }> = ({ id, title, subtitle, children, className = 'py-20' }) => (
    <motion.section
        id={id}
        className={`container mx-auto px-6 ${className}`}
        initial={{ opacity: 0, y: 50 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true, amount: 0.3 }}
        transition={{ duration: 0.7 }}
    >
        <h2 className="text-3xl font-bold text-center text-gray-800">{title}</h2>
        <p className="text-center text-gray-600 mt-2 mb-12">{subtitle}</p>
        {children}
    </motion.section>
);


const Services: FC = () => {
  const cardVariants = {
    hidden: { opacity: 0, y: 30 },
    visible: { opacity: 1, y: 0, transition: { duration: 0.5 } }
  };

  return (
    <Section
      id="servicos"
      title="Nossos Serviços"
      subtitle="Oferecemos uma gama completa de tratamentos para garantir sua saúde e estética bucal."
    >
      <div className="grid md:grid-cols-3 gap-8">
        {servicesData.map((service, index) => (
          <motion.div 
            key={index}
            className="bg-white p-8 rounded-lg shadow-lg text-center hover:shadow-xl hover:-translate-y-2 transition-all duration-300"
            variants={cardVariants}
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true, amount: 0.5 }}
          >
            <div className="flex justify-center mb-4">{service.icon}</div>
            <h3 className="text-xl font-bold text-gray-800 mb-2">{service.title}</h3>
            <p className="text-gray-600">{service.description}</p>
          </motion.div>
        ))}
      </div>
    </Section>
  );
};

const About: FC = () => {
  return (
    <Section id="sobre" title="Conheça a nossa clínica" subtitle="Um espaço pensado para o seu conforto e bem-estar." className="py-20 bg-gray-50">
       <div className="container mx-auto px-6 grid md:grid-cols-2 gap-12 items-center">
        <motion.div
            initial={{ opacity: 0, scale: 0.9 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true }}
            transition={{ duration: 0.7 }}
        >
            {/* Use uma imagem real do dentista ou da clínica */}
            <img src="/dentist-profile.jpg" alt="Foto do Dentista" className="rounded-lg shadow-xl" />
        </motion.div>
        <motion.div
            initial={{ opacity: 0, x: 50 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.7, delay: 0.2 }}
        >
            <h3 className="text-2xl font-bold text-gray-800 mb-4">Dr(a). Seu Nome</h3>
            <p className="text-gray-600 mb-4">
                Com mais de 15 anos de experiência, nossa missão é combinar tecnologia, arte e ciência para criar sorrisos saudáveis e bonitos. Acreditamos em um atendimento personalizado, onde cada paciente é único e merece toda a nossa atenção e cuidado.
            </p>
            <p className="text-gray-600">
                Nossa clínica foi projetada para oferecer um ambiente acolhedor e relaxante, equipado com o que há de mais moderno em odontologia, para garantir tratamentos mais rápidos, seguros e confortáveis.
            </p>
        </motion.div>
       </div>
    </Section>
  );
};

const Testimonials: FC = () => {
  return (
    <Section id="depoimentos" title="O que nossos pacientes dizem" subtitle="A satisfação de quem confia no nosso trabalho é a nossa maior recompensa.">
        <div className="grid lg:grid-cols-3 gap-8">
            {testimonialsData.map((testimonial, index) => (
                <motion.div 
                    key={index}
                    className="bg-white p-6 rounded-lg shadow-lg"
                    initial={{ opacity: 0, y: 50 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    viewport={{ once: true, amount: 0.5 }}
                    transition={{ duration: 0.5, delay: index * 0.1 }}
                >
                    <p className="text-gray-600 italic">"{testimonial.quote}"</p>
                    <div className="mt-4">
                        <p className="font-bold text-gray-800">{testimonial.name}</p>
                        <p className="text-sm text-cyan-600">{testimonial.role}</p>
                    </div>
                </motion.div>
            ))}
        </div>
    </Section>
  );
};

const FAQ: FC = () => {
  const [openIndex, setOpenIndex] = useState<number | null>(null);

  const toggleFAQ = (index: number) => {
    setOpenIndex(openIndex === index ? null : index);
  };

  return (
    <Section id="faq" title="Perguntas Frequentes" subtitle="Tire suas dúvidas mais comuns sobre nossos tratamentos e serviços." className="py-20 bg-gray-50">
        <div className="max-w-3xl mx-auto">
            {faqData.map((faq, index) => (
                <div key={index} className="border-b border-gray-200 py-4">
                    <button
                        onClick={() => toggleFAQ(index)}
                        className="w-full flex justify-between items-center text-left text-lg font-semibold text-gray-800"
                    >
                        <span>{faq.question}</span>
                        <motion.div
                            animate={{ rotate: openIndex === index ? 180 : 0 }}
                            transition={{ duration: 0.3 }}
                        >
                            <FaChevronDown className="text-cyan-500" />
                        </motion.div>
                    </button>
                    <AnimatePresence>
                        {openIndex === index && (
                            <motion.div
                                initial={{ opacity: 0, height: 0 }}
                                animate={{ opacity: 1, height: 'auto' }}
                                exit={{ opacity: 0, height: 0 }}
                                transition={{ duration: 0.3, ease: "easeInOut" }}
                                className="overflow-hidden"
                            >
                                <p className="pt-4 text-gray-600">{faq.answer}</p>
                            </motion.div>
                        )}
                    </AnimatePresence>
                </div>
            ))}
        </div>
    </Section>
  );
}

const Contact: FC = () => {
    return (
        <Section id="contato" title="Entre em Contato" subtitle="Estamos prontos para atender você. Agende sua avaliação!">
            <div className="grid md:grid-cols-2 gap-12 items-start">
                <motion.div 
                    className="bg-white p-8 rounded-lg shadow-lg"
                    initial={{ opacity: 0, x: -50 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    viewport={{ once: true }}
                    transition={{ duration: 0.7 }}
                >
                    <h3 className="text-2xl font-bold text-gray-800 mb-6">Informações de Contato</h3>
                    <div className="space-y-4 text-gray-700">
                        <p className="flex items-center"><MdLocationOn className="w-6 h-6 mr-3 text-cyan-500"/> Av. Principal, 123 - Centro, Sua Cidade</p>
                        <p className="flex items-center"><MdPhone className="w-6 h-6 mr-3 text-cyan-500"/> (99) 99999-9999</p>
                        <p className="flex items-center"><MdEmail className="w-6 h-6 mr-3 text-cyan-500"/> contato@seudentista.com.br</p>
                    </div>
                     <div className="mt-8">
                        <h4 className="font-bold text-gray-800 mb-3">Horário de Funcionamento:</h4>
                        <p className="text-gray-700">Seg - Sex: 08:00 - 18:00</p>
                        <p className="text-gray-700">Sáb: 09:00 - 13:00</p>
                    </div>
                    <a 
                        href="https://wa.me/5500000000000" // Coloque seu número de WhatsApp aqui
                        target="_blank"
                        rel="noopener noreferrer"
                        className="mt-8 w-full text-center inline-block bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-8 rounded-full text-lg transition-transform transform hover:scale-105 shadow-lg"
                    >
                       <FaWhatsapp className="inline-block mr-2 mb-1" /> Agendar pelo WhatsApp
                    </a>
                </motion.div>
                <motion.div 
                    className="rounded-lg shadow-lg overflow-hidden h-full"
                    initial={{ opacity: 0, x: 50 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    viewport={{ once: true }}
                    transition={{ duration: 0.7 }}
                >
                     {/* Placeholder para um mapa. Você pode incorporar um Google Maps aqui */}
                     <img src="/map-placeholder.png" alt="Localização da clínica no mapa" className="w-full h-full object-cover min-h-[400px]" />
                </motion.div>
            </div>
        </Section>
    )
}

const Footer: FC = () => {
    return (
        <footer className="bg-gray-800 text-white">
            <div className="container mx-auto px-6 py-8">
                <div className="flex flex-col md:flex-row justify-between items-center">
                    <p>&copy; {new Date().getFullYear()} Dr(a). Seu Nome. Todos os direitos reservados.</p>
                    <div className="flex space-x-4 mt-4 md:mt-0">
                        <a href="#" className="hover:text-cyan-400 transition-colors"><FaInstagram size={24} /></a>
                        <a href="#" className="hover:text-cyan-400 transition-colors"><FaFacebook size={24} /></a>
                        <a href="#" className="hover:text-cyan-400 transition-colors"><FaWhatsapp size={24} /></a>
                    </div>
                </div>
            </div>
        </footer>
    );
};


// --- Página Principal ---
const LandingPage: FC = () => {
  return (
    <>
      <Head>
        <title>Dr(a). Seu Nome - Clínica Odontológica</title>
        <meta name="description" content="Clínica odontológica especializada em clareamento, implantes e saúde bucal. Agende sua consulta e transforme seu sorriso." />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <div className="bg-white font-sans">
        <Header />
        <main>
          <Hero />
          <Services />
          <About />
          <Testimonials />
          <FAQ />
          <Contact />
        </main>
        <Footer />
      </div>
    </>
  );
};

export default LandingPage;